# RunPod worker SDK
runpod>=1.7.13

# Used directly by handler.py — kept minimal. ltx-core / ltx-pipelines
# bring their own deps in via setup install (transformers, accelerate,
# safetensors, einops, scipy, av, openimageio, tqdm, pillow). No need
# to repeat them here.
huggingface_hub>=0.26.0
hf_transfer>=0.1.8
Pillow>=10.4.0
